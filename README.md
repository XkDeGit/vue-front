# 订阅管理系统 (Subscription Management System)

基于 Vue 3 + TypeScript + Element Plus 的现代化订阅管理前端应用。

## 🎨 特性

- ✅ **Vue 3 + TypeScript** - 类型安全的现代前端框架
- ✅ **Element Plus** - 优雅的 UI 组件库
- ✅ **Vue Router** - 官方路由解决方案
- ✅ **Pinia** - 直观的状态管理
- ✅ **TanStack Query** - 强大的数据获取和缓存
- ✅ **vue-i18n** - 国际化支持（中文/英文）
- ✅ **Vee-Validate + Zod** - 表单验证
- ✅ **Axios** - HTTP 客户端
- ✅ **Vite** - 极速的开发体验
- ✅ **卡通风格UI** - 柔和温馨的设计风格

## 📦 快速开始

### 安装依赖

```bash
npm install
```

### 开发环境

```bash
npm run dev
```

访问 http://localhost:3000

### 生产构建

```bash
npm run build
```

### 预览生产构建

```bash
npm run preview
```

## 🛠️ 代码质量

### 类型检查

```bash
npm run type-check
```

### Lint

```bash
npm run lint
```

### 格式化

```bash
# 检查格式
npm run format

# 修复格式
npm run format:write
```

### 测试

```bash
# 监听模式
npm run test

# 单次运行
npm run test:unit
```

## 📁 项目结构

```
src/
├── api/              # API 模块
│   ├── http.ts      # Axios 配置
│   ├── auth.ts      # 认证 API
│   └── types.ts     # 类型定义
├── assets/          # 静态资源
├── components/      # 全局组件
├── composables/     # 组合式函数
├── layouts/         # 布局组件
│   └── MainLayout.vue
├── locales/         # 国际化
│   ├── en/         # 英文
│   └── zh/         # 中文
├── router/          # 路由配置
├── stores/          # Pinia 状态管理
│   └── auth.ts     # 认证状态
├── styles/          # 全局样式
│   ├── variables.scss
│   └── global.scss
├── views/           # 页面视图
│   ├── LoginView.vue
│   ├── DashboardView.vue
│   └── SubscriptionsView.vue
├── App.vue          # 根组件
└── main.ts          # 入口文件
```

## 🎨 主题定制

项目使用卡通风格的淡黄色主题：

- 背景色：#fff7e0
- 强调色：#ffc94a
- 文字色：#1f2937

详细的设计规范请参考 [CLAUDE.md](./CLAUDE.md)

## 🌐 国际化

支持中英文切换，在右上角点击语言按钮即可切换。

语言文件位于 `src/locales/` 目录。

## 🔐 认证流程

- JWT Token 认证
- 自动 Token 刷新机制
- 路由守卫保护
- sessionStorage 持久化

## 🚀 部署

### Nginx 配置

参考 `nginx.conf.example` 文件。

基本步骤：

1. 构建项目：`npm run build`
2. 将 `dist/` 目录内容复制到 Nginx 服务器
3. 配置 Nginx（参考示例配置）
4. 重启 Nginx

## 📝 开发规范

- 使用 Conventional Commits 规范提交信息
- 提交前运行 `npm run lint && npm run test:unit`
- 保持代码简洁、可读、可维护
- 遵循 TypeScript 严格模式

## 📄 License

MIT
